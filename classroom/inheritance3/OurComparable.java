package inheritance3;

public interface OurComparable{
    /**
     *
     * @param o
     * @return positive negative 0
     * 但是这个没用了，因为Java中有自带的comparable接口
     */
    public int compareTo(Object o);
}

